# L0 第六轮复审（2026-09-23）

评审范围：第五轮 R5-1（空 Coordinator 绑定导致提交事务跳过身份比较），并复核 R4-1/R4-2 的既有竞争路径。评审方独立运行真实 ASGI/SQLite 探针，检查绑定入口、旧库记录读取与事务比较；未修改运行时代码或测试基线。

## 结论

**R5-1 可以关闭，本次代码修复通过评审。** 绑定入口拒绝空 Coordinator，上传入口拒绝旧库中的空绑定，事务内无条件比较 Coordinator/Attempt/worker 身份。原 `l0_r5_repro.py` 在建立空绑定时即抛出 `input_mismatch`，因此改用[第六轮独立探针](l0_r6_repro.py)单独检查三层防护。

| 验证路径 | 结果 |
|---|---|
| 新建空 Coordinator 绑定 | `input_mismatch`，未写入 |
| 注入旧库空绑定后上传 | 409 `stale_assignment`；artifact 数 0 |
| 事务复检传入空预期值、当前值非空 | `stale_assignment`，未跳过比较 |
| 正常 Attempt 改绑 | 409；artifact 数 0 |
| 正常 Coordinator 改绑 | 409；artifact 数 0 |
| 等待 SQLite 写锁时租约过期 | 409；artifact 数 0 |

历史 R3 复现仍符合预期：同 key 并发 `[201, 201]`，相同业务 DTO 排版变化 201，已过期租约与上传中取消 409，消息仅追踪字段变化 202/202，未知 session 回执 409。

## 验证与范围

- HTTP 契约专项：**69 passed**。
- 独立全量 **810 passed / 9 skipped / 0 failed / 0 errors**（437.80 秒）；`scripts/check_full_suite.py --input .pytest_l0_r6_report.txt` **exit 0**。未刷新测试基线；探针输出、源码指纹和全量日志摘要见[证据 JSON](2026-09-23-l0-r6-evidence.json)。
- 规范包 `tools/validate.py` 与宿主 `tools/check_evidence.py --require-host-audit` 均 exit 0。
- 本结论仅关闭 R5-1 代码缺陷。HCZJ H/messages 错误映射与原 S1 reviewer 归属仍按此前记录开放；T1–T6 全开放，BINDING-GATE-1 继续关闭，未声明 wire conformance。
